// crear arrays (2)
let miArray =  [];
let miArray2 = new Array();

const numeros = [1, 2, 3, true, 'str']
console.log(numeros)
console.table(numeros)

// agregar datos con la 2da forma
const dias = new Array ('lunes', 'martes', 'miercoles');
console.log(dias)

// puede tener datos varadiados
let variado = ['a', 'e', 'i', 2, 4, dias, {nombre : 'Pablo', lenguaje : 'Ts'}]
console.log(variado)

// acceder a los valores
console.log(numeros[1])